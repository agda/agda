load_silently currentFile []
top_command currentFile (cmd_infer_toplevel Normalised "Bar")
-- Reload to test that pragmas are respected when interface files are
-- used.
load_silently currentFile []
top_command currentFile (cmd_infer_toplevel Normalised "Bar")
