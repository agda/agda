-- This file should mirror the flags for GHC 9.0.2 enabled in
-- `Agda.cabal`. It should be loaded automatically by the `.ghci` file
-- in this directory.

:script .ghci-9.0
:set -Wunicode-bidirectional-format-characters
