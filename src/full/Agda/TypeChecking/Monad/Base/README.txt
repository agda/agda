Andreas, 2014-03-22

This directory contains data structures used in the type checking monad (TCM).

On the long run, we might want to split the monolithic Monad.Base into
smaller modules collected here.
